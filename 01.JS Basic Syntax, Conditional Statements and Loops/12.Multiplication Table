function solve(input){

  let number = Number(input);
  let times = 0;
  let product = number * times;
    for(let i = 1;i <= 10;i++){
      product = number * i;
      console.log(`${number} X ${i} = ${product}`)
    }
}
solve([2])
